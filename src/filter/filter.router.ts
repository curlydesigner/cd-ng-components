import { FilterComponent } from './filter.component';
import { AppRoutes } from '../app/app-route';

export const filterRouter: AppRoutes = [{
    path: 'filter',
    component: FilterComponent,
    title: 'Filter'
  }];
