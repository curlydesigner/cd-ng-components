import { AppRoutes } from '../app/app-route';
import { SearchSelectComponent } from './search-select.component';

export const searchSelectRouter: AppRoutes = [{
    path: 'search-select',
    component: SearchSelectComponent,
    title: 'Search Select'
  }];
